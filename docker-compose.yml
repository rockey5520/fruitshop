version: '3.4'

services:
  backend:
    image: fruitshop
    expose:
      - "8080"
    ports:
      - "8080:8080"
  frontend:
    image: fruitshop-ui
    expose:
      - "80"
    ports:
      - "4200:80"
    depends_on:
      - backend